## 2026-06-14 19:10 (channel: #cyber-dev)
- **User:** Showed screenshot — the ilammy/msvc-dev-cmd fix didn't work, the windows build is still failing at `findVisualStudio`. Same `find-visualstudio.js:118` error.
- **You:** Root cause: as of June 8-15 2026, GitHub is migrating `windows-latest` to Windows Server 2025 with VS 2026 (runner-images#14017). The `ilammy/msvc-dev-cmd` action doesn't recognize VS 2026 (version 18.5), so it silently doesn't set up the dev cmd env. Two-pronged fix: (1) pin `runs-on` to `windows-2022` which still has VS 2022 Build Tools at a known path — unblocks today. (2) Use `vswhere.exe` to auto-detect whatever VS is actually installed and source its vcvarsall.bat — works for any VS version (2017+, including 2026) so the same workflow works when we eventually migrate off windows-2022. vcvarsall.bat is now sourced in the same cmd shell that runs electron-rebuild, so the env vars are guaranteed visible to the rebuild subprocess. Moved v3.1.7 tag to include this commit. Pushed.
## 2026-07-22 17:31 (channel: #cyber-dev)
- **User:** Tobe asked to remove chat noise from mobile feed/play ("the comments the companion makes when given food or played with does not need to appear in the chat") and reported the quest edit still fails ("Couldn't update quest: quest not found").
- **You:** v3.2.16 ships two changes. (1) `mobile-arena-treat-placed` and `mobile-arena-treat-eaten` IPC handlers no longer call `promptCompanionReaction` (which triggered an LLM roundtrip + chat message). They now only log via `addDesktopLog` — no chat noise, no LLM call, no companion reply bubble. Mobile's 😋 + ❤️ emoji overlay (handled by the mobile arena, not the desktop) is the only user-visible feedback. The desktop's own tap-to-treat flow (placeTreatOnArena in src/js/app.js:4905) is unaffected — same action, different context, different feedback policy. (2) Added `available: [quest ids]` to the `quests_update_failed` response from sync-server.js. New `onListQuests` callback returns `loadQuests().map(q => q.id)`. Server-side log: `[SyncServer] update_quest: id not found. requested: X available: [a, b, c]`. Mobile's failedHandler now appends the diagnostic to the toast. v3.10.73's gate was a reasonable hypothesis but didn't fix it — rather than guess at a second fix, instrumented for the next test run to reveal the actual root cause. Bumped to v3.2.16. Pushed to origin. See CHANGES_3.2.16.md for the lessons "instrument-before-fix when your hypothesis was wrong" + "different UX surfaces need different noise levels — same action, different context, different feedback policy".

## 2026-07-22 17:31 (channel: #cyber-dev)
- **User:** Tobe asked to remove chat noise from mobile feed/play ("the comments the companion makes when given food or played with does not need to appear in the chat") and reported the quest edit still fails ("Couldn't update quest: quest not found").
- **You:** v3.2.16 ships two changes. (1) `mobile-arena-treat-placed` and `mobile-arena-treat-eaten` IPC handlers no longer call `promptCompanionReaction` (which triggered an LLM roundtrip + chat message). They now only log via `addDesktopLog` — no chat noise, no LLM call, no companion reply bubble. Mobile's 😋 + ❤️ emoji overlay (handled by the mobile arena, not the desktop) is the only user-visible feedback. The desktop's own tap-to-treat flow (placeTreatOnArena in src/js/app.js:4905) is unaffected — same action, different context, different feedback policy. (2) Added `available: [quest ids]` to the `quests_update_failed` response from sync-server.js. New `onListQuests` callback returns `loadQuests().map(q => q.id)`. Server-side log: `[SyncServer] update_quest: id not found. requested: X available: [a, b, c]`. Mobile's failedHandler now appends the diagnostic to the toast. v3.10.73's gate was a reasonable hypothesis but didn't fix it — rather than guess at a second fix, instrumented for the next test run to reveal the actual root cause. Bumped to v3.2.16. Pushed to origin. See CHANGES_3.2.16.md for the lessons "instrument-before-fix when your hypothesis was wrong" + "different UX surfaces need different noise levels — same action, different context, different feedback policy".

